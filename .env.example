# Site URL — used for canonical tags, OG meta, and sitemap
# Set to your production URL when deploying (e.g., https://cleanspark-virid.vercel.app)
PUBLIC_SITE_URL=http://localhost:4321

# Default theme when no cookie is set (minimal | bold | trust | bubbly | noir)
PUBLIC_DEFAULT_THEME=minimal
